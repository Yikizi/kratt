# openWakeWord comparison artifacts (2026-05-15)

Combined supervisor-style artifacts for comparing the April microWakeWord benchmark table with the 2026-05-15 openWakeWord diagnostic runs.

Files:

- `benchmark_results_supervisor_plus_openwakeword.csv` — existing supervisor benchmark CSV plus four OWW full-run CSVs.
- `supervisor_table_plus_openwakeword.md` / `.csv` — pivot at threshold `0.97` for the standard supervisor columns.
- `supervisor_report_plus_openwakeword.html` — interactive HTML report with threshold selector.

Interpretive note: `docs/research/openwakeword-framework-comparison-2026-05-15.md`.

Caveat: this is a diagnostic framework comparison using matching benchmark families. It is not a full proof that the frameworks were trained under equivalent recipes.
