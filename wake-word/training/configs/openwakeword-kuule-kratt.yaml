# openWakeWord training config for "Kuule Kratt"
# Uses our own pre-recorded WAV files (no Piper TTS generation)
# Run: python -m openwakeword.train --training_config THIS_FILE --augment_clips --train_model

target_phrase:
  - "kuule kratt"

model_name: "kuule_kratt"
output_dir: "/gpfs/mariana/smbhome/malinh/kratt-data/training/openwakeword"

# Not used (we skip --generate_clips) but required by config parser
piper_sample_generator_path: "/dev/null"
n_samples: 2000
n_samples_val: 500
tts_batch_size: 1
custom_negative_phrases:
  - "kuule rott"
  - "kuule kraam"
  - "kuule kraft"
  - "hei kratt"
  - "tere kratt"

# Augmentation settings
augmentation_rounds: 5
augmentation_batch_size: 500

# Background audio for mixing (MUSAN noise + music)
background_paths:
  - "/gpfs/mariana/smbhome/malinh/kratt-data/datasets/musan/musan/noise"
  - "/gpfs/mariana/smbhome/malinh/kratt-data/datasets/musan/musan/music"
background_paths_duplication_rate:
  - 1
  - 1

# Room impulse responses (MIT IR survey - check if downloaded)
rir_paths:
  - "/gpfs/mariana/smbhome/malinh/kratt-data/datasets/benchmarks/mit-impulse-responses/16khz"

# Training
model_type: "dnn"
layer_size: 64
steps: 50000
batch_n_per_class: 512
max_negative_weight: 100
target_false_positives_per_hour: 0.2

# Validation data for false positive tracking during training
false_positive_validation_data_path: "/gpfs/mariana/smbhome/malinh/kratt-data/processed/faph_test_cv_et"

# Feature files (auto-set by augment_clips, but needed for train_model)
feature_data_files:
  positive: null
  adversarial_negative: null
