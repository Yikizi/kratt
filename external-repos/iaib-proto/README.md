# iaib-proto migration note

This path used to be a Git submodule pointing to the standalone `iaib` repository.

As of 2026-02-12, the project structure has been consolidated so the current monorepo
layout is the source of truth, and the GitLab `iaib` remote is updated from this repo.

Historical commit history remains available in Git history and in the original GitLab
project history.
