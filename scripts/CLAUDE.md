# scripts/

Utility scripts for demos, deployment, and setup. Not part of the CLI — these
are standalone helpers.

## Structure

```
scripts/
├── demo/               # Live demo overlays and display scripts
├── deployment/         # ESPHome flash, serial port tools, model prep
└── setup/              # One-time environment setup (install_esphome.sh)
```
